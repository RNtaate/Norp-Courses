<div class="card">
  <div class="card-header">
    <h2>Editing Lesson</h2>
    <small>
      <b>Course: </b>
      <%= @course.title %>
    </small>
  </div>

  <div class="card-body">
    <%= render 'form', course: @course, lesson: @lesson %>
  </div>

  <div class="card-footer">
    <%= link_to 'Cancel', course_lesson_path(@course, @lesson), class: "btn btn-sm btn-danger mt-3" %>
  </div>
</div>