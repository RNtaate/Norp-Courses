<h1 class="display-6">Courses: <%= @courses.count %></h1>

<div class="row row-cols-1 row-cols-md-3 g-4">
  <% @courses.each do |course| %>
      <%= render 'courses/course', course: course %>
  <% end %>
</div>